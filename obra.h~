#pragma once

#include <sstream>
#include <string>

using std::string;
using std::stringstream;

class Obra{
		string nombre;
		string autor;
		string ingreso;
    public:
		Obra(string,string,string);
		Obra(const Obra&);
		virtual~Obra();
		virtual string toString() const;
		string getNombre();
        string getAutor();
        string getFecha();
};


